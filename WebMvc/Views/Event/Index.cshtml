
@{
    ViewData["Title"] = "Event";
    @model WebMvc.ViewModel.EventIndexViewModel
}
    <div id="myCarousel" class="carousel slide" data-ride="carousel" data-interval="6000">
        <ol class="carousel-indicators">
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
            <li data-target="#myCarousel" data-slide-to="1"></li>
            <li data-target="#myCarousel" data-slide-to="2"></li>
            <li data-target="#myCarousel" data-slide-to="3"></li>
        </ol>
        <div class="carousel-inner" role="listbox">
            <div class="item active">
                <img src="~/images/bannerimage1.jpg" alt="ASP.NET" class="img-responsive"  width="678" height="338"/>
                <div class="carousel-caption" role="option" color="#00FFFF";>
                    <p>
                        Inhale the future, exhale the past.
                    </p>
                </div>
            </div>
            <div class="item">
                <img src="~/images/bannerimage2.png" alt="Visual Studio" class="img-responsive" width="678" height="338"/>
                <div class="carousel-caption" role="option">
                    <p color ="#1B0804">
                        If you can DREAM it, you can DO it.
                    </p>
                </div>
            </div>
            <div class="item">
                <img src="~/images/bannerimage3.jpg" alt="Package Management" class="img-responsive" width="678" height="338"/>
                <div class="carousel-caption" role="option">
                    <p>
                        Break the rules to find a better YOU!
                    </p>
                </div>
            </div>
            <div class="item">
                <img src="~/images/bannerimage6.jpg" alt="Package Management" class="img-responsive" width="678" height="338"/>
                <div class="carousel-caption" role="option">
                    <p>
                        Music is an outburst of the soul.
                    </p>
                </div>
            </div>
         </div>
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>

    <section class="esh-event-filters">
        <div class="container">
            <form asp-action="Index" asp-controller="Event" method="post">
                <label class="esh-event-label" data-title="Location">
                    <select asp-for="@Model.LocationFilterApplied" asp-items="@Model.Locations" class="esh-event-filter"></select>
                </label>
                <label class="esh-event-label" data-title="EventType">
                    <select asp-for="@Model.TypesFilterApplied" asp-items="@Model.Types" class="esh-event-filter"></select>
                </label>
                <input class="esh-event-send" type="image" src="~/images/arrow-right.svg" />
            </form>
        </div>
    </section>
<div class="container">
    @if (Model.EventDetails.Count() > 0)
    {
        @Html.Partial("_pagination", Model.PaginationInfo)

        <div class="esh-event-items row">
            @foreach (var eventDetails in Model.EventDetails)
            {                                                              
                <div class="esh-event-item col-md-4">
                    @Html.Partial("_product", eventDetails)
                </div>
            }
        </div>

        @Html.Partial("_pagination", Model.PaginationInfo)
    }
    else
    {
        <div class="esh-event-items row">
            THERE ARE NO RESULTS THAT MATCH YOUR SEARCH
        </div>
    }
</div>

